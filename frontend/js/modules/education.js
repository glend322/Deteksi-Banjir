/**
 * SafeRoute - Education Module
 */

class EducationModule {
    constructor(app) {
        this.app = app;
    }

    init() {
        // Education modal bindings if needed
    }
}

if (typeof window !== "undefined") {
    window.EducationModule = EducationModule;
}
